<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.apsmobile.casaportemporadaapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CasaPorTemporadaApp">
        <activity android:name=".activity.DetalheAnuncioActivity">

        </activity>
        <activity android:name=".activity.MinhaContaActivity" />
        <activity android:name=".activity.MeusAnunciosActivity" />
        <activity android:name=".activity.FiltrarAnunciosActivity" />
        <activity android:name=".activity.FormAnuncioActivity" />
        <activity android:name=".activity.autenticacao.RecuperarContaActivity" />
        <activity android:name=".activity.autenticacao.CriarContaActivity" />
        <activity android:name=".activity.autenticacao.LoginActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>